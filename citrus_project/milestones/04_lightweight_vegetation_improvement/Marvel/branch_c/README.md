# Branch C - RGB Edge-Aware Smoothness

Start here:

- `00_BRANCH_C_REPORT.md` - main Branch C result report and current verdict.
- `branch_c_rgb_edge_smoothness/` - copied training code, run scripts, detailed notes, and ignored local results for the Branch C experiment.

Branch C tests whether masked LiDAR depth supervision plus RGB edge-aware disparity smoothness improves the selected hybrid model while keeping inference RGB-only.

Bulky generated result folders are intentionally ignored in the main Lite-Mono repo. A local backup snapshot was created in `C:/Proj/milestone-4-dump`.
